{
  "name": "rohan-traders-static",
  "version": "1.0.0",
  "description": "Static HTML website",
  "scripts": {
    "dev": "serve . -l 3000",
    "build": "mkdir -p dist && cp index.html dist/ && cp -r assets dist/ 2>/dev/null || true",
    "start": "serve dist -l 3000"
  },
  "dependencies": {
    "serve": "^14.2.1"
  }
}
